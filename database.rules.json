{
  "rules": {
    ".read": "auth != null",
    ".write": "auth != null",
    "presence": {
      "$documentId": {
        ".indexOn": ["updatedAt"],
        "$userId": {
          ".read": "auth != null",
          ".write": "auth != null && auth.uid == $userId"
        }
      }
    }
  }
}
